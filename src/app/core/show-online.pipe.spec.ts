import { ShowOnlinePipe } from './show-online.pipe';

describe('ShowOnlinePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowOnlinePipe();
    expect(pipe).toBeTruthy();
  });
});
